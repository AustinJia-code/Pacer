# UDP-Based Custom Transport with Link Emulator

Emulates and benchmarks dangerous packet behavior between a sender and receiver
using a custom UDP-based transport protocol. 

# Build:
```bash
cd build
cmake ..
make
```

# Run:
In separate terminals:
Runner: ```./runner```
Receiver: ```./receiver```